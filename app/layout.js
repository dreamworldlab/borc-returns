import "./globals.css";

export const metadata = {
  title: "Returns Center — Birth Of Royal Child",
  description: "Start a return or exchange",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
